class Measure {
  constructor(name, initials, description, computation, author) {
    this.name = name;
    this.initials = initials;
    this.description = description;
    this.computation = computation;
    this.author = author;
  }
}

export default Measure;
